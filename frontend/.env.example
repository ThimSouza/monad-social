# Contrato Pulse7702Session.sol deployado na Monad Testnet (chain 10143)
VITE_PULSE7702_IMPLEMENTATION=0x0000000000000000000000000000000000000000

# URL do relay (relay-server) — o relayer paga gas; o utilizador não vê pop-up por ação
VITE_RELAYER_URL=http://localhost:8787

# Opcional: RPC só para leituras no browser (default: testnet público)
# VITE_MONAD_RPC_URL=https://testnet-rpc.monad.xyz
